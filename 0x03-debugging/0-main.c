#include "main.h"
/**
 * main-test if the function is poositive or negative
 * Return: if 0 exit properly, non-zero otherwise
 */
int main(void)
{
	int i;
	
	i = 0;
	positive _or_negative(i);

	return (0);
}
